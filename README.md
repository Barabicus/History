# History

Chrome extension that adds quick buttons on YouTube to help manage search history.

## Features

- Adds an inline **✕** button to visible YouTube search suggestions so you can remove entries quickly.
- Adds **+** and **✕** buttons near the YouTube search box:
  - **+** runs the current query (easy way to add it to history).
  - **✕** removes visible suggestion entries matching the current query.

## Install in Chrome

1. Open `chrome://extensions`.
2. Turn on **Developer mode**.
3. Click **Load unpacked**.
4. Select this folder: `/home/runner/work/History/History`.

Then open YouTube and click in the search box to see suggestion history controls.
